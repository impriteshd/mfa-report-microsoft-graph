MFA & SSPR Registration Report (Microsoft Graph)

This project creates a report showing which users in a Microsoft 365 tenant have set up Multi-Factor Authentication (MFA) and Self-Service Password Reset (SSPR).

What it does

The script collects information from Microsoft Graph and helps you understand:

Which users have MFA enabled or registered
Which users have SSPR set up
Overall security readiness across all users in the tenant
